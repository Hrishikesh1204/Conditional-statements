def check_even_odd_number (n):
  if n%2==0:
    return"the number is even"
  else:
    return"the number is odd"

# Get input from the user outside the function
number = int(input("enter a number:"))

# Call the function with the input and print its result
print(check_even_odd_number(number))
